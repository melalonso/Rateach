= form_for @teacher_comment do |f|
  - if @teacher_comment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@teacher_comment.errors.count, "error")} prohibited this teacher_comment from being saved:"
      %ul
        - @teacher_comment.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
