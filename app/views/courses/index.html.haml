%h1 Listing courses

%table
  %thead
    %tr
      %th Name
      %th Eval sum
      %th Eval amount
      %th

  %tbody
    - @courses.each do |course|
      %tr
        %td= course.name
        %td= course.eval_sum
        %td= course.eval_amount

        %td= link_to 'Show', course

%br

= link_to 'New Course', new_course_path
